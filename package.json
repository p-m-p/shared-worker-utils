{
  "name": "shared-worker-workspace",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter example dev",
    "server": "pnpm --filter example server",
    "build": "pnpm --filter shared-worker-utils build && pnpm --filter example build",
    "test": "pnpm --filter shared-worker-utils test",
    "lint": "eslint ."
  },
  "pnpm": {
    "overrides": {
      "vite": "npm:rolldown-vite@7.1.14"
    }
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "eslint": "^9.38.0",
    "typescript-eslint": "^8.46.2"
  }
}
