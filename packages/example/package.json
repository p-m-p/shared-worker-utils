{
  "name": "example",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "worker:dev": "wrangler dev",
    "worker:deploy": "wrangler deploy",
    "worker:types": "tsc --noEmit -p tsconfig.worker.json"
  },
  "dependencies": {
    "shared-worker-utils": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20251120.0",
    "wrangler": "^4.49.1"
  }
}
